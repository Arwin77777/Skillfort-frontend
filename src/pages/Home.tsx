import React from 'react';
import { Box, Typography } from '@mui/material';

const Home: React.FC = () => {
    return (
        <Box p={3}>
            <Typography variant="h4">Welcome to Skillfort</Typography>
        </Box>
    );
};

export default Home;
